# Minitalk

## Project Information

### About
The purpose of this project is to code a small data exchange program using UNIX signals.

### Mandatory
- Produce **server** and **client** executables.
- The client must communicate a string passed as a parameter to the server (referenced by its process ID) which then displays it.
- Use **SIGUSR1** & **SIGUSR2** signals ONLY.

### Bonus
- Add a **reception acknowledgement** system.
- Support **Unicode** characters.

### Allowed Functions
- `malloc`
- `free`
- `write`
- `getpid`
- `signal`
- `sigemptyset` & `sigaddset`
- `sigaction`
- `pause`
- `kill`
- `sleep`
- `usleep`
- `exit`

### Clone
Clone the repository :
   ```bash
   git clone https://github.com/yassinbenmansour/Minitalk.git
   ```

   
